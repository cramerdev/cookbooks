<%= node[:nginx][:log_dir] %>/*.log {
        daily
        missingok
        rotate 52
        compress
        delaycompress
        notifempty
        create 644 root root
        sharedscripts
        postrotate
                /etc/init.d/nginx reload
        endscript
}
