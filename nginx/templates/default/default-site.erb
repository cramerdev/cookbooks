server {
  listen   80;
  server_name  <%= node[:hostname] %> <%= node[:ipaddress] %>;

  access_log  <%= node[:nginx][:log_dir] %>/localhost.access.log;

  location / {
    root   /var/www/nginx-default;
    index  index.html index.htm;
  }
}
