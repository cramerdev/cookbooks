<%- @stats.each do |env, s| %>
<%= env %>:
  app_id: <%= %>
  host: <%= s['host'] || 'localhost' %>
  port: <%= s['port'] || 8125 %>
  mode: <%= s['mode'] || node.chef_environment %>
  default_sample_rate: <%= s['default_sample_rate'] || 1 %>
<%- end %>
